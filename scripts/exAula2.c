#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int tam(char *);
int regr(int n);
char bin(int n);
float pow(x, y);

void main (void) {
	char s[100];
	printf("Type a word:\n");
	gets(s);
}